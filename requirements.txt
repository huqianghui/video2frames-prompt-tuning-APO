# agent-lightning 0.3.1 built from the huqianghui/agent-lightning fork (NOT
# PyPI 0.3.0, which is missing required functionality; the fork also carries
# extra logging/test changes). Pinned to the commit this project was developed
# against. The [apo] extra pulls in poml, which the APO algorithm requires.
# litellm is pinned to the version resolved in the fork's uv.lock; newer sdists
# require a Rust toolchain to build, which fails on plain pip installs.
litellm[proxy]==1.90.0
agentlightning[apo] @ git+https://github.com/huqianghui/agent-lightning.git@9d1a546118a2ac43e5c29c920cc8227a0d5cccac

azure-storage-blob
python-dotenv
openai
httpx
agentops
pytest
